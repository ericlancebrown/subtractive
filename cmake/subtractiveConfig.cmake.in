@PACKAGE_INIT@

set(SUBTRACTIVE_LIBRARIES subtractive)

# Capturing values from configure (optional)

set_and_check(subtractive_INCLUDE_DIR "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")

get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(${SELF_DIR}/subtractive-targets.cmake
